/** Online multiplayer is fully built (see src/lib/room.ts, src/context/OnlineGameContext.tsx)
 * but hidden from the UI for now — flip this back on once a real Redis/Upstash
 * database is wired up in production. */
export const ONLINE_MULTIPLAYER_ENABLED = false;
